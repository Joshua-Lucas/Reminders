import colors from './Colors'

export const Theme = {
  /*-- COLORS --*/

  // Brand Colors
  primaryColor: colors.primary.light,
  secondaryColor: colors.primary.dark,
  tirtiaryColor: colors.accent,
  lightNeutralColor: colors.neutral.light,
  darkNeutralColor: colors.neutral.dark,
  // Text Color

  primaryTextColor: colors.neutral.dark,
  secondarydTextColor: colors.neutral.light,

  // Button Colors
  primaryHoverColor: colors.primary.dark,
  secondaryHoverColor: '#fff',
}

export default Theme
